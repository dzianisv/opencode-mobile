legacy-peer-deps=true
# npm 11 script gate: only @sentry/cli has an install script in this tree
# (downloads the sentry-cli binary for source-map uploads). CI/global npm
# without this key runs all scripts; this keepalist just unblocks it locally.
allow-scripts=opencode-ai,@sentry/cli
